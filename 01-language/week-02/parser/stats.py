"""Статистика по разобранному логу. Модуль 2 из 3.

Здесь нет ни чтения файлов, ни разбора строк — на вход приходят готовые
LogRecord. Каждая функция — чистая: одни и те же данные дают один и тот же
результат, ничего снаружи не меняется. Такие функции тривиально тестировать,
и это главная причина так писать.

Все функции принимают Sequence, а не Iterable: по списку можно пройти
несколько раз, по генератору — нет.
"""

from collections.abc import Sequence

from parsing import LogRecord


# --- Задача 4 ---------------------------------------------------------------
def unique_paths(records: Sequence[LogRecord]) -> set[str]:
    """Вернуть множество всех путей, которые встречались в логе.

    Одна строка через set comprehension.
    """
    raise NotImplementedError


# --- Задача 5 ---------------------------------------------------------------
def requests_per_path(records: Sequence[LogRecord]) -> dict[str, int]:
    """Посчитать количество запросов к каждому пути.

    На прошлой неделе ты делал это через `dict.get(key, 0) + 1`. Здесь
    попробуй `collections.Counter` — и сравни, что читается лучше.
    Возвращать обычный dict, не Counter (`dict(counter)`).
    """
    raise NotImplementedError


# --- Задача 6 ---------------------------------------------------------------
def count_by_status_class(records: Sequence[LogRecord]) -> dict[str, int]:
    """Сгруппировать запросы по классу кода ответа: '2xx', '3xx', '4xx', '5xx'.

    Классы, которых в логе нет, в результат не попадают. 204 -> '2xx',
    503 -> '5xx'. Считать через целочисленное деление: `status // 100`.

    count_by_status_class(...) -> {'2xx': 12, '5xx': 1}
    """
    raise NotImplementedError


# --- Задача 7 ---------------------------------------------------------------
def error_rate(records: Sequence[LogRecord]) -> float:
    """Доля ответов 5xx среди всех запросов — число от 0.0 до 1.0.

    Пустой лог -> 0.0. Делить на len(records) без проверки нельзя:
    ZeroDivisionError. Это та же привычка «сначала отсекаем крайний случай».
    """
    raise NotImplementedError


# --- Задача 8 ---------------------------------------------------------------
def slowest_paths(records: Sequence[LogRecord], limit: int) -> list[tuple[str, float]]:
    """Вернуть limit путей с наибольшим СРЕДНИМ временем ответа.

    Порядок: по убыванию среднего времени; при равенстве — по имени пути
    по алфавиту. Среднее не округлять.

    Это самая большая задача проекта: сначала сгруппируй времена по путям,
    потом посчитай среднее, потом отсортируй. Три шага — три выражения,
    не пытайся уложить в одну строку.

    slowest_paths(recs, 2) -> [('/api/report', 830.0), ('/api/users', 45.5)]
    """
    raise NotImplementedError


# --- Задача 9 ---------------------------------------------------------------
def busiest_hour(records: Sequence[LogRecord]) -> str | None:
    """Вернуть час суток с наибольшим числом запросов, как '09'. Пустой лог -> None.

    Час — это символы 11:13 в timestamp вида '2026-08-17T09:14:02'. Дату
    игнорируем: интересует именно час суток. При равенстве — более ранний час.

    Разбирать время через срез строки — приём для учебной задачи. В проде
    для этого есть `datetime`, до него дойдём на неделе 4.
    """
    raise NotImplementedError
